# Vector Database & Embedding
sentence-transformers
chromadb
# torch is installed separately with CPU-only version in Dockerfile

# API Framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# Utilities
tqdm
python-dotenv
requests