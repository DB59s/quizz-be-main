/**
 * Constants for User Service API
 * Defines all enum values used across the application
 */

// User roles
const ROLES = {
  STUDENT: 'student',
  TEACHER: 'teacher',
  ADMIN: 'admin'
};

module.exports = {
  ROLES
};
